node_modules/
dist/
build/
out/
.next/
.turbo/
.cache/
coverage/
# Blob reports of the sharded CI test runs (scripts/ci-test-shard.sh).
.vitest-reports/
*.log

.env
.env.local
.env.*.local

.DS_Store
.vscode/
.idea/
*.swp
*.swo

apps/bridge/bin/
apps/bridge/panel-host-bridge
apps/bridge/dist/

docker/squad-mock-depot/content/

# Experiment artifacts outside repo path should never appear here, but guard.
/squad-experiment/
*.tsbuildinfo

# Playwright artifacts
apps/web/playwright-report/
apps/web/test-results/

# Git worktrees
.worktrees/

# Runtime state (postgres/redis/caddy/depot/backups/servers) — created by
# install-host-bridge.sh + docker-compose bind-mounts, never source.
/data/

# Stryker mutation testing
.stryker-tmp/
**/reports/mutation/

.playwright-mcp/
.env.tk104
.sentrux/

# Playwright verification evidence artifacts
apps/web/e2e-evidence/

# Agent enforcement harness: only committed config is source; runtime state
# (session locks, worktrees, done/blocked markers, local overrides) is not.
.claude/*
!.claude/settings.json
