# Project rules — squad-admin-panel

All contributor rules live in @AGENTS.md — branch model (`master` = production, `dev` = integration, work branches off `dev`, **never create a `main` branch**), the merge workflow, the CI gate, and the testing policy. Follow them exactly; they override any conflicting default behavior.
