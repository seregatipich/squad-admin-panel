# Project rules — squad-admin-panel

@./AGENTS.md

Read and follow `AGENTS.md` at the repository root (imported above) before making any change. It defines the mandatory branch model (`master` = production, `dev` = integration, work branches off `dev`, **never create a `main` branch**), the merge workflow, the CI gate, and the testing policy.
