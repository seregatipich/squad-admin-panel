# worker-automation — API surface

P2 stub. No public surface exists.

When implemented, this worker will consume `events:server:{id}` Redis Streams via a consumer group and may write command results back to a dedicated stream or Postgres table.
