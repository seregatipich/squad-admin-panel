# worker-automation — Testing

P2 stub. Test suite runs with `--passWithNoTests`. No test files exist.

When implemented, tests must cover rule evaluation, action dispatch, and Redis consumer-group membership.
