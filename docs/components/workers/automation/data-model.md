# worker-automation — Data model

P2 stub. No data is read or written.

When implemented, this worker will read user-defined automation rules from a Postgres table (schema TBD) and consume `events:server:{id}` Redis Streams.
