# worker-stats — API surface

P2 stub. No public surface exists.

When implemented, this worker will consume the `stats:v1` consumer group from `events:server:{id}` streams (see `CONSUMER_GROUP.stats` in `packages/shared-types/src/events.ts`).
