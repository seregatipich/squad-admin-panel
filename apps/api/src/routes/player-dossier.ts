import type { FastifyPluginAsync } from 'fastify';

/**
 * DOSSIER-5 (#192): consolidated read-only player dossier endpoint.
 */
const playerDossierRoutes: FastifyPluginAsync = async () => {};

export default playerDossierRoutes;
